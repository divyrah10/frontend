'use client'

import { useAuthStore } from '@/stores/auth'

export default function SettingsPage(){const user=useAuthStore(s=>s.user);return <div><div className="mb-8"><p className="text-xs uppercase tracking-[.2em] text-accent-400 mb-2">Workspace</p><h1 className="text-4xl">Settings</h1></div><div className="grid lg:grid-cols-2 gap-6"><section className="bg-white border border-accent-100 p-7"><h2 className="text-2xl mb-6">Signed-in administrator</h2><dl className="space-y-4 text-sm"><div><dt className="text-accent-400">Name</dt><dd>{user?.full_name}</dd></div><div><dt className="text-accent-400">Email</dt><dd>{user?.email}</dd></div><div><dt className="text-accent-400">Role</dt><dd className="capitalize">{user?.role.name.replaceAll('_',' ')}</dd></div></dl></section><section className="bg-white border border-accent-100 p-7"><h2 className="text-2xl mb-6">Store configuration</h2><p className="text-sm text-accent-600 leading-relaxed">Payment, storage, email and security secrets are intentionally managed through server environment variables. They are never exposed in this browser panel.</p><div className="mt-6 p-4 bg-accent-50 text-xs text-accent-500">Use <code>backend/.env</code> for private service settings and restart the API after changes.</div></section></div></div>}
